apiVersion: v2
name: nullnode-root
description: >-
  GitOps entrypoint. Installs the AppProject and the single root Application
  that owns everything else - the last thing applied imperatively.
type: application
version: 1.0.0
appVersion: "1.0.0"
