apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "open-webui.fullname" . }}
  labels:
    {{- include "open-webui.labels" . | nindent 4 }}
    nullnode.io/component: chat-ui
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "open-webui.selectorLabels" . | nindent 6 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        {{- include "open-webui.labels" . | nindent 8 }}
        nullnode.io/component: chat-ui
    spec:
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      initContainers:
        - name: wait-for-litellm
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -eu
              for _ in $(seq 1 60); do
                if wget -q --spider --timeout=3 {{ .Values.litellm.gatewayUrl }}/models; then
                  echo "litellm gateway reachable"; exit 0
                fi
                echo "waiting for litellm gateway..."; sleep 5
              done
              echo "litellm gateway never became reachable" >&2; exit 1
          resources:
            requests:
              cpu: 20m
              memory: 64Mi
            limits:
              memory: 128Mi
      containers:
        - name: open-webui
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
          env:
            # LiteLLM gateway configuration
            - name: OPENAI_API_BASE_URL
              value: {{ .Values.litellm.gatewayUrl | quote }}
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.litellm.authSecret }}
                  key: {{ .Values.litellm.masterKeyKey }}
            # Default department for new users
            - name: DEFAULT_DEPARTMENT
              value: {{ .Values.defaultDepartment | quote }}
            # Disable telemetry
            - name: ANONYMIZED_TELEMETRY
              value: "False"
            # WebUI configuration
            - name: WEBUI_NAME
              value: "NullNode Chat"
          startupProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 5
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      terminationGracePeriodSeconds: 30
