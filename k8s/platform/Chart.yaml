apiVersion: v2
name: nullnode-platform
description: >-
  App-of-apps for NullNode. Every template here is an ArgoCD Application; no
  workload is defined directly. Ordering is expressed with sync waves so the
  CRDs, the datastores and the gateway come up in a sane order.
type: application
version: 1.0.0
appVersion: "1.0.0"
